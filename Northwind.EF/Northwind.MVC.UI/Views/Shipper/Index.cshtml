@model IQueryable<Northwind.MVC.UI.Models.ShipperView>

@{
    ViewBag.Title = "Shipper Page";
}

<h2>@ViewBag.Title</h2>

<div class="row">
    <div class="col-md-12">
        <table class="table table-striped">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">ID</th>
                    <th scope="col">Company Name</th>
                    <th scope="col">Phone</th>
                    <th class="text-center" scope="col">Action</th>
                </tr>
            </thead>

            <tbody>
                @foreach (var shipper in Model)
                {
                    <tr>
                        <th scope="row">@shipper.Id</th>
                        <td>@shipper.Name</td>
                        <td>@shipper.Phone</td>
                        <td class="text-center">
                            <button id="update" type="button" class="btn btn-sm btn-success">@Html.ActionLink("Update", "Insert", new { id = shipper.Id })</button>

                            <button id="delete" type="button" class="btn btn-sm btn-warning">@Html.ActionLink("Delete", "Delete", new { id = shipper.Id }, new { onclick = "return confirm('Are you sure to delete?')" })</button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

        <div class="row" style="padding: 5px;">
            <a href="~/Shipper/Insert" class="btn btn-primary">Insert New Shipper</a>
        </div>

    </div>
</div>